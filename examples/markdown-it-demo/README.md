# markdown-it example

A Node script that pipes a sample markdown document through markdown-it +
`@boceto/markdown-it` and prints the resulting HTML.

## Run

From the repo root:

```bash
pnpm install
pnpm build
pnpm --filter @boceto/example-markdown-it-demo start
```

That runs `node index.mjs` in this folder.
